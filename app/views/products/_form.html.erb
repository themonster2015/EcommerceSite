<%= form_for(product) do |f| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name, size: 50 %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description, cols: 51, rows: 3 %>
  </div>

  <div class="field">
    <%= f.label :image_url %>
    <%= f.text_field :image_url, size: 50 %>
  </div>
  <div class="field">
  <%= f.label :color %><br>
  <%= f.text_field :color, size: 50 %>
  </div>
  <div class="field">
  <%= f.label :price %><br>
  <%= f.text_field :price , size: 50 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
